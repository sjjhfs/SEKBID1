# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Ensuring at least one instance is always warm for instant availability
  minInstances: 1
  maxInstances: 2
  memory: 1Gi
